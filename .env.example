## Model Relay Desktop Tauri 环境配置（必须设置）
# 可通过 `MTGA_ENV_FILE` 指定不同的 `.env` 路径（变量名保留为兼容字段）。

# 模块来源：auto | local | root
# - auto: 优先 python-src/modules，找不到再回退仓库根
# - local: 强制 python-src/modules
# - root: 强制仓库根 modules
MTGA_MODULES_SOURCE=local

# 路径严格模式：1=禁止回退，0=允许回退
MTGA_PATH_STRICT=1

# 资源目录（可选，留空则自动探测 modules/resources）
MTGA_RESOURCE_DIR=
